name: abyadpos_tab
description: "A new Flutter project."

publish_to: 'none'

version: 3.1.5+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
#  flutter_thermal_printer: 1.1.0
#  thermal_printer_plus: ^1.0.8
#  flutter_pos_printer_platform_image_3: ^1.2.4
  sunmi_printerx: ^1.2.1
  
#  dartarabic: ^0.3.1
#  charset_converter: ^2.1.1
  flutter_terminal_sdk:
    path: "./sdk/flutter-terminal-sdk"
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  connectivity_plus: ^6.1.0
  datetime_setting: ^2.0.3
  permission_handler: ^11.0.1
  flutter_time_guard: ^1.2.6
  app_settings: ^7.0.0
  device_info_plus: ^11.2.2
  intl:
  image_picker: ^1.0.5
#  mobile_scanner: ^6.0.10
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  image: ^4.1.3
  dio: ^5.9.0
#  flutter_app_installer: ^1.0.2
  nfc_manager: ^4.0.0
  open_filex: ^4.4.0
  package_info_plus: ^8.3.1
  jwt_decoder: ^2.0.1
  json_annotation: ^4.8.1
  skeletonizer: ^2.0.0
  sunmi_printer_plus: ^4.0.0
  bloc:
  flutter_bloc:

  hive:
  flutter_localizations:
    sdk: flutter
  lottie: ^1.0.1
  dotted_border:
  video_player:
  mask_text_input_formatter:
  path_provider:


  # sunmi_printer_plus: 3.0.1

#  sunmi_printer_plus:
#    git:
#     url: https://github.com/brasizza/sunmi_printer.git

#  flutter_sunmi_printer:
#     git:
#       url: https://github.com/bonaparta13/flutter_sunmi_printer.git
#       ref: master  # Or use a specific branch/commit

#  flutter_sunmi_printer:
#    path: ./flutter_sunmi_printer

  url_launcher: ^6.2.6
  esc_pos_utils_plus: ^2.0.3
  json_serializable: ^6.7.1
  injectable_generator: ^2.4.1
  get: ^4.6.5
  usb_serial: ^0.5.2
  android_intent_plus: ^5.2.0
  toastification: ^3.0.3
#  webview_flutter: ^4.0.7        # latest stable
#  webview_flutter_android: ^4.7.0
  google_fonts: 
  flutter_easyloading:
  flutter_screenutil:
  shared_preferences:
  persistent_bottom_nav_bar:
  circular_profile_avatar: ^2.0.5
  fl_chart: ^0.71.0
  responsive_ui:
  place_picker_v2: ^0.0.1
  syncfusion_flutter_datepicker: ^29.1.38
  http:

  connectivity_wrapper:
  flutter_typeahead: ^5.2.0
  syncfusion_flutter_datagrid:
  data_table_2:
  timeline_tile: ^2.0.0

  pdf:
  syncfusion_flutter_pdfviewer: 29.1.38
  printing:
  dotted_line:
  qr_flutter: ^4.1.0
  flutter_barcode_scanner_plus: ^3.0.8

  change_app_package_name: ^1.5.0
  firebase_core: ^4.6.0
  



  geolocator: any
  flutter_pos_printer_platform_image_3_sdt: ^1.2.12
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/images/png/dummy_logo.png"
  min_sdk_android: 28
  web:
    generate: false
    image_path: "path/to/image.png"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: false
    image_path: "path/to/image.png"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: false
    image_path: "path/to/image.png"


  flutter_lints: ^3.0.0
  sunmi_printer:

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/svg/
    - assets/images/png/
    - assets/images/json/
    - assets/config/version.json
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Epilogue
      fonts:
        - asset: assets/fonts/Epilogue-Black.ttf
          weight: 900
        - asset: assets/fonts/Epilogue-Bold.ttf
          weight: 700
        - asset: assets/fonts/Epilogue-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Epilogue-Medium.ttf
          weight: 500
        - asset: assets/fonts/Epilogue-Regular.ttf
          weight: 400
        - asset: assets/fonts/Epilogue-SemiBold.ttf
          weight: 600

    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
        - asset: assets/fonts/Cairo-Bold.ttf
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
